import styled from "styled-components";

const NavigationWrapper = styled.div`
  flex: 0.2;
`;

const ContentWrapper = styled.div`
  flex: 0.8;
`;

export const Styled = { NavigationWrapper, ContentWrapper };
